scalar JSON

scalar Date

scalar Time

scalar DateTime

scalar ObjectId

scalar EmailAddress

scalar PhoneNumber

scalar DocumentNumber

scalar URL

enum Currency {
  BRL
}

type File {
  name: String!
  mimeType: String!
  url: URL!
}
